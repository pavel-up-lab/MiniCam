# План выпадающего календаря архива

1. Добавить `ArchiveCalendarAvailabilityTests` для доступного дня, доступного часа и выбора ближайшего момента в разрыве; пройти короткие циклы red-green.
2. Реализовать чистый `ArchiveCalendarAvailability`, использующий переданный `Calendar` и список `RecordingSegment`.
3. Создать отдельный `ArchiveCalendarPopover` с календарём месяца, навигацией, сеткой часов, выбором минут и кнопкой перехода.
4. Добавить кнопку календаря отдельным верхним слоем в `CameraPlayerView`, не меняя нижнюю панель, и подключить переход через существующие preview-transition и `AppContainer.seek(to:)`.
5. Проверить обновление доступности при изменении `recordingSegments`, пустой архив и закрытие выпадающего окна.
6. Запустить минимальные unit-тесты, общую сборку и визуальную проверку на текущем Mac; после подтверждения собрать универсальную версию для Intel и Apple Silicon.
